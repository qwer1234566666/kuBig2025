[project]
name = "uv-test"
version = "0.1.0"
description = "uv 를 테스트한다.."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
test_package_main = "main:main"

[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"